type TPermission = {
    join: boolean
    general: boolean
    unit: boolean
    event: boolean
    doc: boolean
    member: boolean
    post: boolean
    finance: boolean
    approval: boolean
}

export default TPermission
